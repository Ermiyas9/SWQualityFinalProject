<Window x:Class="GroundTerminalApp.logsPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GroundTerminalApp"
        mc:Ignorable="d"
        Title="logsPage" Height="450" Width="800">
    <Grid>
        <!-- placing the status controls at the bottom just for checking  -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,20">
            <!-- this is the connection status at the bottom -->
            <Label x:Name="connectionStatusLbl"
                   Content="Connection Status"
                   FontSize="22"
                   FontWeight="Bold"
                   Foreground="Red"
                   Margin="0,0,20,0"/>
            <CheckBox x:Name="connStatChkBx"
                      Content="OFFLINE"
                      Foreground="Red"
                      Height="30"
                      Width="120"/>
        </StackPanel>
    </Grid>
</Window>
