<Window x:Class="GroundTerminalApp.logsPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="System Logs" Height="750" Width="1200"
        Background="#1E1E2F" FontFamily="Arial" FontSize="16">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar Navigation -->
        <StackPanel Background="#333333" Grid.Column="0" Margin="15,100,35,310">
            <TextBlock Text="FDMS"
                       HorizontalAlignment="Left"
                       FontSize="34"
                       FontWeight="Bold"
                       Foreground="White"
                       Background="#333333"
                       Height="76"
                       VerticalAlignment="Center"/>

            <Button Content="Search &amp; Query" Height="76" FontSize="20" FontWeight="Bold"
                    Background="#333333" Foreground="White" HorizontalContentAlignment="Left"/>
            <Button Content="System Logs" Height="76" FontSize="20" FontWeight="Bold"
                    Background="#333333" Foreground="White" HorizontalContentAlignment="Left"/>
            <Button Content="User"  Height="76" FontSize="20" FontWeight="Bold"
                    Background="#333333" Foreground="White" HorizontalContentAlignment="Left"/>
        </StackPanel>

        <!-- Main Content -->
        <Grid Grid.Column="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Page Header -->
            <TextBlock Text="System Logs"
                       Foreground="White"
                       FontSize="42"
                       FontWeight="Bold"
                       Margin="0,0,0,20"/>

            <!-- Control Panel -->
            <Border Grid.Row="1" Background="#2A2A40" Padding="15" CornerRadius="8" Margin="0,0,0,15">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBox x:Name="TxtSearch"
                             Width="300"
                             Height="35"
                             Margin="0,0,15,0"
                             Background="#444"
                             Foreground="White"
                             Padding="8"
                             Text="Search logs..."/>

                    <ComboBox x:Name="CmbLogLevel" Width="150" Height="35" Margin="0,0,15,0">
                        <ComboBoxItem Content="ALL"/>
                        <ComboBoxItem Content="INFO"/>
                        <ComboBoxItem Content="WARNING"/>
                        <ComboBoxItem Content="ERROR"/>
                        <ComboBoxItem Content="CRITICAL"/>
                    </ComboBox>

                    <Button Content="Refresh"
                            Width="120"
                            Height="35"
                            Background="#4CAF50"
                            Foreground="White"
                            FontWeight="Bold"/>

                    <Button Content="Clear Logs"
                            Width="120"
                            Height="35"
                            Margin="10,0,0,0"
                            Background="#D32F2F"
                            Foreground="White"
                            FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <!-- Logs Table -->
            <Border Grid.Row="2" Background="#1F1F30" CornerRadius="10" Padding="10">
                <DataGrid x:Name="LogsDataGrid"
                          AutoGenerateColumns="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None"
                          Background="#1F1F30"
                          Foreground="Black"
                          RowBackground="#2A2A40"
                          AlternatingRowBackground="#24243A"
                          CanUserAddRows="False">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp}" Width="200"/>
                        <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="120"/>
                        <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="200"/>
                        <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
                    </DataGrid.Columns>

                </DataGrid>
            </Border>
        </Grid>

        <!-- Remote Database Connection block -->
        <StackPanel Grid.Column="0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Bottom"
                    Margin="0,10,0,10">

            <!-- Title on top -->
            <Label x:Name="connForDBStatusTitle1"
                   Content="Remote DB Connection"
                   FontSize="20"
                   Foreground="White"
                   FontWeight="Bold"
                   HorizontalAlignment="Left" />

            <!-- Icon + OFFLINE text side-by-side -->
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,8,0,0">

                <Image x:Name="dbOnlineIcon"
                       Source="https://img.icons8.com/tiny-color/16/online.png"
                       Width="48"
                       Height="48"
                       Visibility="Collapsed" />

                <Image x:Name="dbOfflineIcon"
                       Source="https://img.icons8.com/3d-fluency/94/wi-fi-disconnected.png"
                       Width="48"
                       Height="48"
                       Visibility="Visible" />

                <Label x:Name="dbConnectionStatusLbl"
                       Content="OFFLINE"
                       FontSize="18"
                       Foreground="Red"
                       VerticalAlignment="Center"
                       Margin="10,0,0,0" />
            </StackPanel>
        </StackPanel>

    </Grid>

</Window>
